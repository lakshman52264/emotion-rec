import modules/functions i have come across till now 

label encoder(sklearn) convert categorical coumn to numerical column
nltk (punkt module)-tokinaztion of text
ast-ast.literal_eval( converts string to list)
sklearn.feature_extraction.text - CountVectorizer(used for bow)
sklearn.metrics.pairwise - cosine_similarity(distance between vectors) 1 if both are near 
enumerate - returns tuple with index values